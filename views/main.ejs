<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>
<div id="main">
    <% if(User){ %>
        <div>
            <label for="id">닉네임</label>
            <label for="id"><%= User.nickname %></label>
        </div>
        <a href="/auth/logout">로그아웃</a>
        <%if(User.login_type==='kakao'){%>
            <a href="/auth/kakao_logout">카카오계정과 함께 로그아웃</a>
        <%}%>
        <a href="/profile/<%= User.nickname %>">프로필</a>
        <% if(BoardList){ %>
            <div>
                <label>게시판 목록</label>
            </div>
            <% for(let list_of_board of BoardList) { %>
                <div>
                    <a href="/board/<%= list_of_board.dataValues.id %>"><%= list_of_board.dataValues.name %></a>
                </div>
            <% } %>
        <% } %>
    <% }else{ %>
        <form id="main-form" action="/auth/login" method="post">
            <div>
                <label for="id">아이디</label>
                <input id="id" name="id" type="text" maxlength="30" required>
            </div>
            <div>
                <label for="password">비밀번호</label>
                <input id="password" name="password" type="password" maxlength="15" required>
            </div>
            <button id="login" type="submit">로그인</button>
            <a href="/auth/join">회원가입</a>
            <a href="/auth/kakao">카카오톡</a>
            <% if(BoardList){ %>
                <div>
                    <label>게시판 목록</label>
                </div>
                <% for(let list_of_board of BoardList) { %>
                    <div>
                        <a href="/board/<%= list_of_board.dataValues.id %>"><%= list_of_board.dataValues.name %></a>
                    </div>
                <% } %>
            <% } %>
        </form>
    <% } %>
</div>
</body>
</html>