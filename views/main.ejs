<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>
<div id="main">
    <% if(Session.passport){ %>
        <div>
            <label for="id">아이디</label>
            <label for="id"><%= Session.passport.user %></label>
        </div>
        <a href="/auth/logout">로그아웃</a>
        <button id="register_button" type="submit">프로필</button>
    <% }else{ %>
        <form id="main-form" action="/auth/login" method="post">
            <div>
                <label for="id">아이디</label>
                <input id="id" name="id" type="text" maxlength="30" required>
            </div>
            <div>
                <label for="password">비밀번호</label>
                <input id="password" name="password" type="password" maxlength="15" required>
            </div>
            <button id="login" type="submit">로그인</button>
        </form>
    <% } %>
</div>
</body>
</html>