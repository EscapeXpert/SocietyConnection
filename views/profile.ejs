<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= User.nickname + " " + title %></title>
</head>
<body>
<div id="profile">
    <div>
        <label for="nickname">닉네임</label>
        <label for="nickname"><%= User.nickname %></label>
    </div>
    <div>
        <label for="sns_id">sns_id</label>
        <label for="sns_id"><%= User.sns_id %></label>
    </div>
    <div>
        <label for="name">이름</label>
        <label for="name"><%= User.name %></label>
    </div>
    <div>
        <label for="birth_date">생년월일</label>
        <label for="birth_date"><%= birth %></label>
    </div>
    <div>
        <label for="gender">성별</label>
        <% if(User.gender){ %>
            <label for="gender">남성</label>
        <% }else{ %>
            <label for="gender">여성</label>
        <% } %>
    </div>
    <div>
        <label for="introduce">자기소개</label>
        <label for="introduce"><%= User.introduce %></label>
    </div>
    <% if(User.profile_image){ %>
        <div>
            <label for="profile_image">profile_image</label>
        </div>
        <div>
            <img id="img-preview" src=<%= User.profile_image %>  width="250" alt="미리보기">
        </div>
    <% } %>
    <% if(User.nickname == req_User.nickname){ %>
        <div>
            <a href="/profile/<%= User.nickname %>/change_password">비밀번호 변경</a>
            <a href="/profile/<%= User.nickname %>/edit">프로필 수정</a>
            <a href="/profile/<%= User.nickname %>/my_activity">나의 활동</a>
            <a href="/message/receive" onclick="window.open(this.href, '_blank', 'width=600, height=600'); return false;">쪽지함</a>
            <a href="/profile/<%= User.nickname %>/account_delete">회원탈퇴</a>
        </div>
    <% } %>
</div>
</body>
</html>